---
 - hosts: 20.113.63.42
   become_user: azureuser
   tasks:
   - name: Clone a github repository
     git:
       repo: https://github.com/entando/entando-pipelines.git
       dest: /home/azureuser/Project/test
       clone: yes
       update: yes
       version: experiment/ansible-mock

   - name: Run Script
     shell: | 
       cd /home/azureuser/Project/test/entando-pipeline/macro/
       ./ansible-mock-script {{ ticket_number }} 
     register: output

   - name: Print Results Tickets
     debug:
       msg: "Ticket number {{output }}"
